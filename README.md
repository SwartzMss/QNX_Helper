# QNX_Helper

一个基于 MkDocs Material 的 QNX 学习资料与实践记录仓库。

## 内容方向

- QNX 入门与基础概念
- 常用命令与开发实践
- 编译、部署和调试记录
- 踩坑与问题排查总结

## 文档结构

文档站点内容位于 `docs/`，站点配置位于 `mkdocs.yml`。

## 部署方式

仓库配置为在 `main` 分支有提交时，由 GitHub Actions 自动构建并部署到 `gh-pages` 分支。

如果仓库的 GitHub Pages 已设置为从 `gh-pages` 分支发布，就不需要在本地手动生成文档。
